/*
 *
 */
package de.netzmuffel.bank;

/**
 * The Class App implements the main-function and is the entry-point.
 */
public class App {

	/**
	 * The main method, which starts the program.
	 *
	 * @param args
	 *            the arguments
	 */
	public static void main(String[] args) {
		Bootstrap bt = new Bootstrap();
		bt.execute(args);
	}

}
